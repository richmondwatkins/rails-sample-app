<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %> <!-- renders the _user partial -->
  <% end %>
</ul>

<%= will_paginate %>
